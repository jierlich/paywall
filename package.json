{
  "name": "y",
  "version": "0.0.1",
  "description": "Lightweight payments tooling for access to digital assets",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jierlich",
  "license": "MIT",
  "devDependencies": {
    "hardhat": "^2.3.0"
  }
}
